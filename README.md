# SETUP

- `homebrew` installieren: [https://brew.sh](https://brew.sh)
- `brew install mise`
- `mise install`
- `bundle config set --local without 'development test'`
- `bundle install`
- `npm install`
- `npm run build`
